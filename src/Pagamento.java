// Interface que define o contrato para métodos de pagamento
public interface Pagamento {
    // Método para processar um pagamento com um valor específico
    void processarPagamento(double valor);
}
